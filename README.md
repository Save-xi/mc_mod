# mc_mod

基于 `deep-research-report.md` 的 Fabric 1.20.1 原型实现（v0.1）：

- Trinkets `legs/belt` 槽位数据包与 `sky_belt` 标签。
- 服务器权威风压飞行状态机（IDLE/BOOST/GLIDE/COOLDOWN）。
- C2S/S2C 网络包（双击空格请求 + HUD 同步）。
- 客户端 `DrawContext` HUD 风压条。
